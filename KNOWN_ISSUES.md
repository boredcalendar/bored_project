# Known Issues

No known code-quality backlog is currently documented.

Use `vp check` for formatting, lint, and type checks. Use `vp run knip` for
unused files, exports, and dependencies. If a future issue is intentionally
deferred, document the reason here or in the relevant tool config with a TODO.
